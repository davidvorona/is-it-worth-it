import React from 'react';
import ReactDOM from 'react-dom';
import App from './app';
import '../styles.css';

ReactDOM.render(
  <App />,
  document.getElementById('root')
);

/* start dev server with 'webpack-dev-server' */
